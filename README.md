cake-markup-language
====================

The Cake Markup Language (CML) is a CakePHP plugin which provides further abstraction of the View layer by replacing the procedural PHP code with an XML based markup.
